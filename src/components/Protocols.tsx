import React from 'react';

const Protocols: React.FC = () => {
  return (
    <div>
      <h1>Protocols</h1>
      <p>Details about various protocols used in the platform.</p>
      {/* Add information about protocols here */}
      <h2>Protocol 1: HTTP</h2>
      <p>HTTP (Hypertext Transfer Protocol) is a protocol used for transferring data over the web.</p>
      <h2>Protocol 2: FTP</h2>
      <p>FTP (File Transfer Protocol) is a protocol used for transferring files over the web.</p>
    </div>
  );
};

export default Protocols;
